import React from "react"

export default function Update() {
    console.log("Update page has been called!")

    return (
        <React.Fragment>
        <h1>
            Update!
        </h1>
        </React.Fragment>
    );
}