import React from "react"

export default function Delete () {
    console.log("Delete page has been called!")

    return (
        <React.Fragment>
        <h1>
            Delete!
        </h1>
        </React.Fragment>
    );
}