import React, { useEffect, useState } from "react"
import { Button } from 'react-bootstrap';
import axios from "axios";
import './display.css';

export default function List() {
    console.log("List all page has been called!")

    return (
        <React.Fragment>
            <h1>
                Work in progress...
            </h1>
        </React.Fragment>
    );
}