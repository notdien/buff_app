import React from "react"

export default function Search() {
    console.log("Search page has been called!")

    return (
        <React.Fragment>
        <h1>
            Work in progress...
        </h1>
        </React.Fragment>
    );
}